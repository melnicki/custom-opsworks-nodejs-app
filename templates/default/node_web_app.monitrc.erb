check host localhost with address 127.0.0.1
  start "/sbin/start <%= @application_name %>"
  stop "/sbin/stop <%= @application_name %>"
  if failed port 80 protocol HTTP
    request /status
    with timeout 10 seconds
    then restart